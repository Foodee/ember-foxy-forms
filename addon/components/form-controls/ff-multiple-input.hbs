{{#each this._inputs as |item|}}
  <input
    class="input-{{item.id}}"
    type="text"
    value={{get @value item.key}}
    ...attributes
    data-test-multiple-input
    {{on "change" (fn this.handleValueChange item.key)}}
    {{on "keyUp" (fn this.handleValueChange item.key)}}
  />
{{/each}}